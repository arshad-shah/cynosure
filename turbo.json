{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["tsconfig.base.json", "biome.json"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "inputs": [
        "src/**",
        "tokens/**",
        "scripts/**",
        "package.json",
        "tsconfig.json",
        "tsup.config.ts",
        "style-dictionary.config.mjs"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"],
      "inputs": ["src/**", "tests/**", "vitest.config.*"]
    },
    "test:watch": {
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@arshad-shah/cynosure-tokens#typecheck": {
      "dependsOn": ["^build", "build:tokens"],
      "outputs": []
    },
    "@arshad-shah/cynosure-tokens#build": {
      "dependsOn": ["^build", "build:tokens"],
      "outputs": ["dist/**"],
      "inputs": ["src/**", "package.json", "tsconfig.json", "tsup.config.ts"]
    },
    "build:tokens": {
      "outputs": ["src/generated/**", "dist/css/**"],
      "inputs": ["tokens/**", "scripts/**", "style-dictionary.config.mjs"]
    },
    "clean": { "cache": false },
    "@arshad-shah/cynosure-docs#build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "src/generated/**"],
      "inputs": [
        "src/**",
        "scripts/**",
        "public/**",
        "astro.config.mjs",
        "tsconfig.json",
        "package.json"
      ]
    }
  }
}
