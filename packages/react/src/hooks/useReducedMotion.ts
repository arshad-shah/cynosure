// Phase 03 owns the implementation; re-exported here so consumers can import
// every hook from `@lumen/react/hooks` without round-tripping through theme.
export { useReducedMotion } from '../theme/hooks/useReducedMotion.js';
