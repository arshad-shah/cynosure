---
interface Props { size?: number; class?: string }
const { size = 4, class: className } = Astro.props;
---
<svg width={size} height={size} viewBox="0 0 8 8" fill="currentColor" aria-hidden="true" class:list={[className]}>
  <circle cx="4" cy="4" r="2" />
</svg>
