---
title: Slider
description: Single-thumb slider built on React Aria; supports marks, value readout, and locale-aware formatting.
category: components
status: stable
since: "0.1.0"
a11y: Thumb exposes role="slider" with aria-valuemin/max/now; arrow keys move by step, Home/End jump to bounds.
bundleSize: "—"
tags: [forms]
---
import LivePreview from '@/components/LivePreview/LivePreview.astro';
import PropsTable from '@/components/PropsTable.astro';
import StatusBadge from '@/components/StatusBadge.astro';
import BundleSizePill from '@/components/BundleSizePill.astro';

<div><StatusBadge status="stable" /> <BundleSizePill name="Slider" /></div>

<LivePreview example="slider/basic" />

## Usage

```tsx
import { Slider } from "@arshad-shah/cynosure-react";

<Slider label="Volume" defaultValue={50} minValue={0} maxValue={100} />
```

## Sizes

Three track sizes are available: `sm`, `md` (default), and `lg`.

<LivePreview example="slider/sizes" />

## With value

Set `showValue` to render the current value in the header.

<LivePreview example="slider/with-value" />

## Step

Use `step` to constrain motion to discrete increments.

<LivePreview example="slider/step" />

## Marks

Pass an array of `{ value, label }` to render tick marks beneath the track.

<LivePreview example="slider/marks" />

## Formatted

Pass `formatOptions` to format the value as currency, percent, or other units.

<LivePreview example="slider/formatted" />

## Disabled

<LivePreview example="slider/disabled" />

## API

<PropsTable component="Slider" />

## Accessibility

- The thumb exposes `role="slider"` with `aria-valuemin`, `aria-valuemax`, and `aria-valuenow`.
- Arrow Left/Right (or Up/Down) move by `step`; Page Up/Down move by a larger increment; Home/End jump to bounds.
- When `label` is a string it is forwarded as `aria-label` on the thumb.
- Always provide a `label` or `aria-label` so the slider's purpose is announced.

## Recipes

- Use whole-number `step` for counts; fractional `step` for percentages or weights.
- Pair `showValue` with `formatOptions` to keep the readout consistent across locales.
- Use `marks` to surface meaningful waypoints (e.g. tier breakpoints) along the track.
