---
title: Checkbox
description: Toggle a boolean value or participate in a multi-select group. Built on Radix UI with full keyboard and screen-reader support.
order: 4
category: components
status: stable
since: 0.1.0
a11y: Uses Radix UI checkbox root; role="checkbox" with aria-checked including indeterminate; focus ring follows tokens.
bundleSize: "10 kB"
tags: [form, input]
---
import LivePreview from '@/components/LivePreview/LivePreview.astro';
import PropsTable from '@/components/PropsTable.astro';
import StatusBadge from '@/components/StatusBadge.astro';
import BundleSizePill from '@/components/BundleSizePill.astro';

<div><StatusBadge status="stable" /> <BundleSizePill name="Checkbox" /></div>

<LivePreview example="checkbox/basic" />

## Usage

```tsx
import { Checkbox } from '@arshad-shah/cynosure-react';

<Checkbox>Accept terms</Checkbox>
```

## Checked state

<LivePreview example="checkbox/checked" />

## Indeterminate

Use `checked="indeterminate"` (or the shorthand `indeterminate` prop) for partially-selected states.

<LivePreview example="checkbox/indeterminate" />

## Disabled state

<LivePreview example="checkbox/disabled" />

## Group

Use `CheckboxGroup` to manage a set of checkboxes as a multi-select value array.

<LivePreview example="checkbox/group" />

## API

<PropsTable component="Checkbox" />

## Accessibility

- Built on Radix UI checkbox — exposes `role="checkbox"` with proper `aria-checked` including `"mixed"` for indeterminate.
- Focus ring is token-driven; keyboard activation works via Space key natively.
- `disabled` is forwarded to the Radix root, blocking both pointer and keyboard interaction.

## Recipes

- Use `CheckboxGroup` with `name` when submitting inside a native `<form>` — each checked item submits its `value`.
- Use `indeterminate` on a "select all" parent checkbox to reflect partial selection.
