---
title: Radio
description: Single-select radio button, always used inside a RadioGroup which manages roving-tabindex and keyboard navigation.
order: 5
category: components
status: stable
since: 0.1.0
a11y: Roving tabindex managed by the group; role="radio" with aria-checked.
bundleSize: "10 kB"
tags: [form, input]
---
import LivePreview from '@/components/LivePreview/LivePreview.astro';
import PropsTable from '@/components/PropsTable.astro';

<LivePreview example="radio/basic" />

## Group

Use `RadioGroup` to manage selection and keyboard navigation across items.

<LivePreview example="radio/group" />

## Disabled

<LivePreview example="radio/disabled" />

## API

<PropsTable component="Radio" />

## Accessibility

- The enclosing `RadioGroup` manages roving tabindex so only the selected item is in the tab order.
- Each item exposes `role="radio"` with `aria-checked` reflecting the current value.
- Arrow keys move selection within the group; Space/Enter select the focused item.

## Recipes

- Provide `orientation="horizontal"` on `RadioGroup` for inline layouts.
- Always supply `aria-label` or `aria-labelledby` on `RadioGroup` so screen readers announce the group purpose.
