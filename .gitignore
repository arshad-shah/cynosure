node_modules
dist
.turbo
**/src/generated
storybook-static
coverage
*.tsbuildinfo
.DS_Store
playwright-report
test-results
.env
.env.local
*.log
.vite

# tsup emits these when bundling an ESM config file for Node to load.
# They're transient build artefacts, never meant to ship or lint.
tsup.config.bundled_*.mjs

# Claude Code local settings — per-machine, not shared.
.claude/

# Superpowers brainstorm scratch — per-session mockups.
.superpowers/
