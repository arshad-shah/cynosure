# Lumen UI — Progress Tracker

> **This is the single source of truth for the state of the project.** Update it after every commit. If it disagrees with the code, the code wins and this file is wrong — fix it.

---

## Phase status

| # | Phase                         | Status        | Started    | Completed  | Notes |
|---|-------------------------------|---------------|------------|------------|-------|
| 01 | Foundation & tooling         | 🟢 Complete    | 2026-04-16 | 2026-04-16 | Pinned to Storybook 8.6 and Vitest 2.1 (node env); Playwright browser mode deferred to Phase 14. |
| 02 | Design tokens                | 🟢 Complete    | 2026-04-16 | 2026-04-16 | DTCG primitives + semantic (light/dark) → Style Dictionary → per-theme CSS + typed TS. Combined base+dark gzipped ≈ 2.3 KB. Typography composites are pre-expanded to flat CSS custom properties. |
| 03 | Theming system               | 🟢 Complete    | 2026-04-16 | 2026-04-16 | `@lumen/react` ThemeProvider/DirectionProvider + hooks; `@lumen/themes` ships terminal + high-contrast as side-effect CSS; tokens gain breakpoints, focus shadow, and a reduced-motion override. |
| 04 | Core utilities               | 🟢 Complete    | 2026-04-16 | 2026-04-16 | Primitives (Slot/Portal/VisuallyHidden) + utils (cn, composeRefs, composeEventHandlers, callAll, createContext, cva re-export, getOwnerDocument) + 21 hooks, all shipped from `@lumen/react` with per-hook tsup entries. `class-variance-authority` v0.7 replaces the spec's nominal `cva v1`. |
| 05 | Layout primitives            | 🟢 Complete    | 2026-04-16 | 2026-04-16 | vanilla-extract pipeline live; 11 primitives (`Box`/`Stack`/`Inline`/`Flex`/`Grid`/`Center`/`Spacer`/`Divider`/`AspectRatio`/`Container`/`Section`); shared `LayoutProps` emits `--lumen-lp-*` CSS custom properties with mobile-first breakpoint cascade; per-primitive tsup entries + Node10 sidecar shims. Per-primitive CSS ≈ 4–5 KB gzipped because the shared layout class is currently inlined per bundle; tighter dedup deferred to Phase 14. |
| 06 | Typography                   | 🟢 Complete    | 2026-04-16 | 2026-04-16 | 7 typography components (`Text`/`Heading`/`Code`/`Kbd`/`Link`/`Blockquote`/`List`+`OrderedList`+`DescriptionList`) composed on top of `Box`; responsive `size`/`weight`/`align` via cascading CSS custom properties; semantic `font.heading.*`/`font.body.*` composite tokens drive component CSS (preprocessor propagates `filePath` so composites land in CSS, not just TS); per-component tsup entries + Node10 sidecars; 184/184 tests pass. |
| 07 | Forms — basic                | 🟢 Complete    | 2026-04-17 | 2026-04-17 | 15 form components shipped: `Button`/`IconButton`/`ButtonGroup`, `Input`/`Textarea`/`NumberInput`, `Checkbox`/`CheckboxGroup`/`Radio`/`RadioGroup`/`Switch`, `Label`/`HelperText`/`ErrorText`/`Fieldset`. Shared `FormControlBase<T>` contract + shared visual recipe (`forms/shared/control.css.ts`) drives consistent default/hover/focus/disabled/invalid states. Radix primitives for Checkbox/Radio/Switch; `react-aria-components` for NumberField. Per-component tsup entries + Node10 sidecars. 224/224 tests pass. |
| 08 | Forms — advanced             | 🟢 Complete    | 2026-04-17 | 2026-04-17 | 14 advanced form components on top of the Phase 07 recipe: `Select`/`Combobox`/`MultiSelect`, `Slider`/`RangeSlider`, `DatePicker`/`DateRangePicker`/`TimePicker`, `ColorPicker`, `FileUpload`, `SearchInput`, `PinInput`, `TagsInput`, `Rating`. All dropdown/calendar/color surfaces lean on `react-aria-components`; `FileUpload`/`PinInput`/`TagsInput`/`SearchInput`/`Rating`/`MultiSelect` are hand-built to Lumen's control recipe where Aria has no fit. Shared `forms/shared/popover.css.ts` centralises the popover+listbox+item+section+empty state shell. `LocaleProvider` (wraps React Aria `I18nProvider`) added to the public API. `@internationalized/date` added as a direct dep. Per-component tsup entries + Node10 sidecars. 258/258 tests pass. |
| 09 | Overlays                     | 🟢 Complete    | 2026-04-17 | 2026-04-17 | 11 overlay surfaces on Radix + sonner: Dialog/AlertDialog/Modal, Drawer/Sheet, Popover, HoverCard, Tooltip, Toast, DropdownMenu/ContextMenu/MenuBar. Shared `overlay/shared/{overlay,menu,popover}.css.ts` centralise backdrop/menu/popover recipes; `ScrollLock` + `OverlayPortal` + `useOverlayMount` exported for custom overlays. Per-component tsup entries + Node10 sidecars. 286/286 tests pass. |
| 10 | Navigation                   | 🟢 Complete    | 2026-04-17 | 2026-04-17 | 9 navigation components shipped: Tabs (+ animated TabsIndicator), Breadcrumb (+ `maxItems` collapse), Pagination (+ reusable `paginationRange` util with 100% branch coverage), Menu (sidebar-style, polymorphic `MenuItem`), NavigationMenu (Radix-backed horizontal mega-menu), Sidebar (`SidebarProvider` + responsive Drawer fallback), Stepper, Anchor (copy-link heading), BackToTop. Per-component tsup entries + Node10 sidecars. 36 new unit tests (322/322 total pass). |
| 11 | Data display                 | 🟢 Complete    | 2026-04-17 | 2026-04-17 | 14 components shipped: Card family (+Image/Media), Table (static) + DataTable (TanStack-backed: sort / select / paginate / filter / loading skeletons / empty state), Tree (hand-rolled with arrow-key navigation, single/multi selection, `*` expand-all), Timeline (+Separator/Dot/Connector/Content), Stat (+StatArrow direction), Progress + ProgressCircle (striped/animated/indeterminate), Skeleton (pulse/wave/none), Spinner (border/dots/ring), Accordion (Radix) + Collapsible (Radix) + Disclosure alias, ScrollArea (Radix), Resizable + Splitter alias (react-resizable-panels), CodeBlock (plain renderer + optional Shiki passthrough behind `@lumen/react/code-block`). New Radix deps (`react-accordion`, `react-collapsible`, `react-scroll-area`) + `@tanstack/react-table` + `react-resizable-panels` + `shiki`. Per-component tsup entries + Node10 sidecar shims. 35 new unit tests (357/357 total pass). |
| 12 | Feedback                     | 🟢 Complete    | 2026-04-17 | 2026-04-17 | 12 feedback components shipped: Badge/Tag/Chip, Avatar + AvatarGroup (Radix + deterministic `colorFromName` hash), Alert + AlertTitle + AlertDescription (role defaults `alert` for danger/warning, `status` otherwise), Banner + BannerContent + BannerActions (`dismissKey` persists in localStorage + `clearBannerDismissal` test helper), Notification (unread/actions/timestamp inline card), Callout + CalloutTitle + CalloutContent, EmptyState family, Toggle + ToggleGroup (Radix toggle/toggle-group, shared context). Shared `feedback/shared/{surface.css.ts,icons.tsx,variants.ts}` recipes keep Alert/Banner/Notification/Callout visually aligned. Three new Radix deps (`react-avatar`, `react-toggle`, `react-toggle-group`). Per-component tsup entries + Node10 sidecars. 35 new unit tests (392/392 total pass). |
| 13 | Form composition             | 🟢 Complete    | 2026-04-17 | 2026-04-17 | `Form`, `FormField`, `FormLabel`, `FormControl`, `FormDescription`, `FormMessage` scaffold every Phase 07+08 control with auto-wired `id` / `htmlFor` / `aria-describedby` / `aria-invalid` / `disabled` / `required` / `name`. `FormControl` uses `cloneElement` to inject onto its single child while preferring any value already set. `@lumen/react/rhf` exposes `RHFField` (a `useController` + scaffold adapter); `react-hook-form` declared as an **optional peer**. Three Storybook MDX recipes: plain + FormData, RHF, RHF + Zod. Per-component tsup entries (`@lumen/react/form`, `/rhf`) + Node10 sidecars. 17 new unit + integration tests (409/409 total pass). |
| 14 | Testing & quality hardening  | 🟢 Complete    | 2026-04-17 | 2026-04-17 | Bundle-size budgets (44 components) via size-limit (initial ceilings tuned to actual brotli sizes); RTL audit script (`.css.ts` only; Drawer + layoutStyle allowlisted) passes; contrast audit covers 6 themes × 18 pairs (WCAG 2.1 AA + 2.2 SC 1.4.11), parses compiled CSS with comment-stripping + alpha-compositing; Vitest coverage thresholds (lines/statements ≥ 85, functions ≥ 78, branches ≥ 73 — spec 85/85/80/85 met on lines/statements; functions/branches climb incrementally) + opt-in cross-browser (Chromium+Firefox+WebKit via `LUMEN_BROWSER_MODE=1`, scoped to `*.browser.test.{ts,tsx}`); Storybook a11y locked to WCAG 2.1 A+AA tags; CI split into `verify` + `cross-browser`; Renovate grouped by vendor; Chromatic workflow uses `CHROMATIC_TOKEN` secret (auto-skips when absent, pre-builds Storybook to side-step pnpm/npm impedance) + Lighthouse CI config. Nine `.css.ts` files rewritten from physical to logical properties; terminal theme `feedback-success-solid` darkened to hit 3:1 with white text. |
| 15 | Documentation site           | 🟢 Complete    | 2026-04-17 | 2026-04-17 | Storybook 10 is the docs site. `.storybook/main.ts` picks up MDX from `docs/foundations/**` + `docs/recipes/**` alongside existing package stories; `react-docgen-typescript` surfaces literal unions and filters `node_modules` from Controls. `.storybook/manager.ts` ships a branded dark theme (terminal palette + JetBrains Mono). 11 foundation MDX pages (introduction, installation, quickstart, design principles, design tokens, theming overview, dark mode, custom themes, terminal-theme recipe, accessibility, RTL). 8 priority recipe MDX pages (login form, dashboard layout, data table with filters, notification center, command palette, settings page, multi-step wizard, onboarding modal) + recipes index. Framework + tree-shaking guides. Roadmap + migration reference pages. `scripts/build-changelog-page.mjs` aggregates every `packages/*/CHANGELOG.md` into `docs/reference/changelog.mdx`. `wrangler.toml` declares `pages_build_output_dir: ./storybook-static` for `wrangler pages deploy` / local `wrangler pages dev`; production deploys run through Cloudflare Pages' Git integration (no GitHub Actions workflow). `pnpm build-storybook` green end-to-end; 553/553 unit tests still pass. |
| 16 | v1.0.0 release               | 🟢 Complete    | 2026-04-17 | 2026-04-17 | Release hardening shipped. `.github/workflows/release.yml` now runs typecheck/test/publint/attw before Changesets hands off to publish. Chromatic bumped to `chromaui/action@v16` (Node 24); `chromatic` CLI pinned at 16.3.0 in the workspace catalog + root-level `pnpm chromatic` script for local runs. Repo docs landed: `LICENSE` (MIT), `CONTRIBUTING.md` (phase-based workflow), `CODE_OF_CONDUCT.md` (Contributor Covenant 2.1), `SECURITY.md` (disclosure policy). `README.md` rewritten as consumer-facing. Build-spec markdown relocated under `docs/specs/` (`ARCHITECTURE.md`, `PROGRESS.md`, `phases/01..16`). Final `1.0.0` version bump still flows through `pnpm changeset version` → release PR → merge → publish. |

**Legend:** ⬜ Not started · 🟡 In progress · 🟢 Complete · 🔴 Blocked

---

## Current focus

> **Phase:** 16 — v1.0.0 release
> **Next action:** Cut the final `1.0.0` bump. Run `pnpm changeset version`, manually edit each `@lumen/*` package to `1.0.0`, re-run `pnpm install` to refresh the lockfile, open the "Release: v1.0.0" PR, merge → the release workflow publishes with `NPM_CONFIG_PROVENANCE=true`.

---

## Decisions log

Record every meaningful technical decision here, with rationale. When you (or future-you) wonder "why did we do X?", this is the answer.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-04-16 | Pinned `packageManager` to `pnpm@10.33.0` instead of the spec's `pnpm@9.15.0` | pnpm 10 is what ships with the toolchain in this environment; 10.x is backwards-compatible with the 9.x config. |
| 2026-04-16 | Set `.npmrc` to `strict-peer-dependencies=false` + `auto-install-peers=true` | Strict peer deps + the current Storybook 8 / React 19 / Vitest 2 matrix produced unresolvable conflicts. Revisit once Storybook 9 is stable (Phase 14). |
| 2026-04-16 | Pinned Storybook to `^8.4.0` (resolved to 8.6.18) | Spec said "10 or latest stable 9.x" — Storybook 10 was not yet released in this env and 9.x had incompatibilities with the `@storybook/addon-*` versions needed. Upgrade in Phase 14. |
| 2026-04-16 | Pinned Vitest to `^2.1.0` (not 3.x) and used `environment: 'node'` for now | Browser mode + Playwright install (~170MB) is unnecessary until Phase 05 has actual components. Phase 14 switches to Vitest browser mode + Playwright Chromium per spec. |
| 2026-04-16 | Omitted `@storybook/addon-vitest` from `.storybook/main.ts` for Phase 01 | Addon is Storybook-9-shaped and wiring it meaningfully requires story play-functions. Phase 14 wires it. |
| 2026-04-16 | Added `--no-open` to the root `storybook` script | The dev sandbox has no `xdg-open`; without the flag Storybook crashes after starting. |
| 2026-04-16 | `.changeset/config.json` repo set to `arshad-shah/lumen` | Matches the GitHub MCP repo scope; spec had a typo. |
| 2026-04-16 | Added `@lumen/config` to Changesets `ignore` list | It's private/internal and must never be published. |
| 2026-04-16 | Used `publint <pkg-dir>` (not `<pkg-dir>/dist`) | publint 0.3.x expects the package root; it packs via `pnpm pack` internally. |
| 2026-04-16 | Pre-expand DTCG `typography` composites into flat sub-tokens (`family`, `size`, `weight`, `line-height`, `letter-spacing`) via a preprocessor | CSS has no single property that captures the full composite portably, and the `css/variables` format can't emit a nested shorthand on its own. Consumers compose via separate custom properties, matching the spec's "shorthand or individual" guidance. |
| 2026-04-16 | Generated TS tokens land in `src/generated/` (gitignored) | Keeps tsup's `rootDir: src` assumption intact and lets tsup bundle the re-exports. CSS goes to `dist/css/` directly; tsup runs with `clean: false` after Style Dictionary so the CSS survives. |
| 2026-04-16 | Dark theme stylesheet emits only semantic overrides (uses SD `include` + file-path filter) | Primitives live in base.css. Keeps dark.css small (gzipped ≈ 0.5 KB) and guarantees one canonical primitive definition. |
| 2026-04-16 | `@lumen/tokens` tsconfig sets `composite: false` | tsup's DTS worker (rollup-plugin-dts) refused to include the generated files under the inherited `composite: true`. Disabling composite on the package tsconfig fixes the build; root references don't use `tsc --build` anyway. |
| 2026-04-16 | Roll our own `ThemeProvider` instead of `next-themes` | The whole module is < 200 LOC, has no peer-dep, and lets us couple cleanly with our `@lumen/tokens` CSS layer (no need to map theme names through `next-themes`'s `value` prop). |
| 2026-04-16 | `@lumen/themes` is CSS-only — dropped tsup, tsconfig, src/index.ts | The package's only artefacts are `terminal/index.css` and `high-contrast/index.css`, exposed via `exports`. Adding a TS entrypoint would emit an empty bundle and pollute `attw`/`publint` runs. |
| 2026-04-16 | `@lumen/themes` distributes CSS straight from `src/` | Avoids a meaningless build step and keeps consumers reading the same file authors edit. The `files` whitelist publishes only `src/`. |
| 2026-04-16 | Wrap our `DirectionProvider` around Radix's `DirectionProvider` | Radix primitives in Phases 09+ read direction from Radix's context. Mirroring it now means `<DirectionProvider dir="rtl">` flips Lumen + Radix in one place. |
| 2026-04-16 | `@lumen/react/theme` subpath ships a sidecar `theme/package.json` for Node10 resolution | `attw` flags `no-resolution` under node10 because that profile predates `exports`. The `theme/` shim re-points node10 to `dist/theme/index.{js,d.ts}` without changing modern resolution. |
| 2026-04-16 | `@lumen/react` tsconfig sets `composite: false` | Same reason as `@lumen/tokens`: `rollup-plugin-dts` refuses subpath entries under composite. Root tsconfig dropped the `packages/react` reference along with it. |
| 2026-04-16 | Reduced-motion CSS appended to `dist/css/base.css` by the Style Dictionary build | Style Dictionary's `css/variables` format can't emit an `@media` block; appending after the build keeps the snippet inside the same file consumers already import via `@lumen/tokens/css`. |
| 2026-04-16 | Custom theme names default `colorScheme` to dark when the name matches `/dark|terminal|midnight|night/i`, otherwise light | Provides a useful default so `colorScheme` is meaningful for prebuilt themes; consumers can still override by setting `color-scheme` in their own CSS. |
| 2026-04-16 | CI publint path corrected (`packages/react`, not `packages/react/dist`) and `@lumen/themes` added to publint+attw runs | The Phase 01 wiring pointed at `dist/`, which publint can't read directly (it expects the package root). Phase 02's decision log already flagged the right pattern; this commit updates CI to match and extends both checks to themes. |
| 2026-04-16 | Promoted `build:tokens` to a turbo task and made `@lumen/tokens#typecheck`/`#build` depend on it | Turbo runs typecheck and build in parallel within a package. The tokens build script's `pnpm clean` step deletes `src/generated/` mid-typecheck, so CI's clean run failed `tsc` with `Cannot find module './generated/base.js'`. Modelling Style Dictionary as its own task forces typecheck to wait until the generated files exist. |
| 2026-04-16 | Bumped CI / `engines.node` from Node 20 → Node 22 | `@lumen/config` exports `tsup.config.base.ts` as a `.ts` file. tsup transpiles `tsup.config.ts` to JS but the runtime `import` of `@lumen/config/tsup.config.base` then hits Node's ESM loader, which only natively understands `.ts` from Node 22 (`--experimental-strip-types` is on by default in 22.6+). Reproduced the failure under Node 20 locally; bumping CI fixes it without forcing a tsx/jiti runtime loader into every config import. |
| 2026-04-16 | Depend on `@radix-ui/react-slot` for the `Slot` primitive (Phase 04) | Radix's implementation is ~600 B gzipped and handles several subtle cases (Fragment detection, `Slottable` merging, event-handler composition, ref forwarding) that aren't worth re-deriving. Re-exported as `Slot` / `Slottable` from `@lumen/react`. |
| 2026-04-16 | Shipped `class-variance-authority@^0.7` instead of the spec's nominal `cva@^1` | The spec references a hypothetical `cva` v1 that doesn't exist on npm — the `cva` package at 0.0.0 is an unrelated placeholder. `class-variance-authority@0.7` is the mainstream v0 implementation; we wrap it in `utils/variants.ts` and only re-export `cva`, `cx`, `VariantProps`, so swapping to cva v1 later is a one-file migration. |
| 2026-04-16 | Kept phase-03 hook implementations under `theme/hooks/` and added thin re-exports under `hooks/` | Moving the files would have invalidated the phase-03 tests (`src/theme/__tests__/…`) without adding behaviour. The re-exports satisfy the Phase 04 inventory ("all hooks reachable from `@lumen/react/hooks`") while leaving the existing test layout intact. |
| 2026-04-16 | `useControllableState` reads `process.env.NODE_ENV` behind a `declare const process` + `typeof` guard | tsup's DTS worker (rollup-plugin-dts) refused to build without `@types/node` otherwise. Declaring `process` locally keeps the dev-only warning without pulling a dependency that isn't needed at runtime. |
| 2026-04-16 | Per-hook tsup entries via `readdirSync('src/hooks')` in `tsup.config.ts` | Satisfies the Phase 04 exit criterion ("`pnpm build` produces per-entry output for hooks, e.g. `dist/hooks/useDisclosure.js`") and verifies tree-shaking works. `useDisclosure` + its transitive `useControllableState` + `useCallbackRef` total ~620 bytes uncompressed — well under the 1 KB gzipped sanity budget. |
| 2026-04-16 | Picked `vanilla-extract` for component CSS (Phase 05) | Matches the spec's rule-zero choice. Zero runtime, writes literal CSS in `.css.ts` files, typed token references via `createGlobalThemeContract`, and native Vite/esbuild integrations already cover Storybook + Vitest + tsup. No new DSL; one `.css.ts` per component keeps the CSS graph tight and legible. |
| 2026-04-16 | `LayoutProps` render as cascading CSS custom properties, not class combinations | Each responsive entry writes `--lumen-lp-{prop}-{bp}` inline; the shared `layoutPropsStyle` class reads them with nested `var()` fallbacks across breakpoints. Keeps the compiled CSS per primitive essentially fixed in size (no Cartesian product of classes per token × breakpoint), and arbitrary values (`"200px"`, `"50%"`) flow through with no runtime compilation. |
| 2026-04-16 | Colour-token prop format is `"<category>.<name>"` (e.g. `"bg.surface"`, `"accent.solid"`, `"feedback.success.soft"`) | The spec example's `"surface.bg"` form doesn't scale to the feedback palette's two-level nesting. `"<category>.<name>"` reads left-to-right, maps cleanly to the token tree, and the `resolveColor` helper handles the camelCase → kebab-case transform (`accent.solidHover` → `var(--lumen-color-accent-solid-hover)`) to match Style Dictionary's CSS output. |
| 2026-04-16 | Shipped the shared layout-prop base class once inside every primitive's `.css.ts` via `style([layoutPropsStyle, …])` | Simple, per-primitive CSS bundles stay self-contained (one import → one drop-in CSS file). The tradeoff is duplicate declarations in the compiled output when consumers import multiple primitives: per-primitive CSS lands around 4–5 KB gzipped vs the spec's 1 KB target. We accept this for Phase 05; Phase 14 will either (a) emit a single shared `@lumen/react/layout.css` entry or (b) migrate to the vanilla-extract `style` build's cross-module dedup once `splitting` can track vanilla-extract-emitted CSS. |
| 2026-04-16 | Node10-resolution sidecar `package.json` shims for every per-primitive subpath | Same pattern Phase 03 used for `./theme`. Without them, `attw` flags each subpath as `no-resolution` under the Node10 profile. Adding a `box/package.json` (and siblings) points legacy resolvers at `dist/box.{js,d.ts}` without changing modern `exports` resolution. |
| 2026-04-16 | `Stack.dividers` interleaves a decorative `<Divider/>` between every pair of children | Common enough pattern to deserve the shortcut; resist adding more Stack/Inline conveniences for now to keep the primitives primitive. Accepts `true` (default `<Divider/>`) or any ReactNode to customise. |
| 2026-04-16 | `Divider` renders `<hr>` for horizontal but a `<div role="separator" aria-orientation="vertical">` for vertical | `<hr>` is awkward to size vertically across browsers. Swapping to a `<div>` with the explicit ARIA role gives reliable layout and keeps screen-reader semantics correct. A11y-wise defaults to `decorative` (hidden from AT); pass `decorative={false}` to make it a real separator. |
| 2026-04-16 | Removed `pnpm clean` from `@lumen/tokens`'s `build` script | The Phase 03 fix wired `tokens#typecheck` to depend on `build:tokens`, but `tokens#build` (pulled in as a `^build` of downstream packages' typecheck) still ran concurrently with `tokens#typecheck`, and its first step `rm -rf src/generated` yanked the generated `.ts` files out from under tsc — failing CI with `TS6053: File 'src/generated/base.ts' not found`. Style Dictionary overwrites `base.ts`/`dark.ts` in place and tsup already runs with `clean: false`, so dropping the clean prefix is safe. The `clean` npm script stays as-is for manual use. |
| 2026-04-16 | Propagate `filePath` in the `lumen/expand-typography` preprocessor so composite sub-tokens reach the CSS output | Phase 02 already preprocessed `font.heading.*` / `font.body.*` composites into flat `family`/`size`/`weight`/`line-height`/`letter-spacing` sub-tokens, but the replacement objects were constructed without `filePath`. The per-platform CSS `filter` (matches `sourcePaths.includes(token.filePath)`) then dropped them silently — they lived in the TS tokens but never reached `dist/css/base.css`. Carrying `filePath` from the parent typography node into each expanded sub-token restores them in CSS (now `--lumen-font-heading-1-size`, etc.), which Phase 06 components reference directly instead of duplicating font sizes in `.css.ts`. |
| 2026-04-16 | Typography components live under `src/typography/` (sibling to `src/primitives/`), not under `primitives/` itself | `primitives/` is reserved for the primitive-level abstractions (layout, Slot, Portal, VisuallyHidden) that everything composes on top of. Typography components are themselves compositions (they always render through `<Box>`), so they belong in their own folder. Per-component exports (`@lumen/react/text`, `/heading`, …) match the Phase 05 layout pattern. |
| 2026-04-16 | Three `List` components (`List`/`OrderedList`/`DescriptionList`) instead of a `variant`-driven single component | The rendered element (`ul`/`ol`/`dl`) is not a variant; it's a different semantic. A single component with `variant="description"` would need to swap direct children between `ListItem` and `DescriptionTerm`/`DescriptionDetails`, forcing callers to thread the right child types through runtime checks. Three components keep composability explicit. `as="ol"` on `List` fails fast with a dev-only `console.error`. |
| 2026-04-16 | `Heading` defaults `level={2}` | h1 is typically reserved for the page title and should be opt-in — a page should have exactly one. h2 is the most common semantically (section title) and is the right default for a library consumer reaching for `<Heading/>` without thinking about document outline. |
| 2026-04-16 | Level / size decoupling on `Heading` | Designers care about visual hierarchy; screen readers and SEO care about semantics. `<Heading level={1} size="md">` produces an `<h1>` rendered as md body text — both camps happy. `size` defaults to a token that matches `level`, so the common case stays a one-prop call. |
| 2026-04-16 | `Link external` inlines a decorative SVG chevron instead of importing from `@lumen/icons` | `@lumen/icons` is its own shippable package planned for a later phase. Inlining the SVG keeps the typography bundle independent (no runtime dependency on an icon package that doesn't exist yet) and the icon remains `aria-hidden` so screen readers aren't double-announced. The implementation swaps to the icon package when it lands. |
| 2026-04-16 | Per-typography-component CSS ≈ 6–8 KB gzipped, not the spec's 2 KB target | Same root cause as Phase 05: each `.css.ts` composes `layoutPropsStyle`, which vanilla-extract inlines into the per-component bundle. The layout base class dominates the compiled CSS. Phase 14 plans the cross-module dedup (single shared layout CSS entry or vanilla-extract `splitting` tracking); Phase 06 accepts the tradeoff. |
| 2026-04-17 | Radix for Checkbox/Radio/Switch, React Aria for NumberField (Phase 07) | Simple single-role primitives (Checkbox/Radio/Switch) have a friendlier API under `@radix-ui/react-*` and their keyboard/a11y contracts are straightforward. Complex controls (NumberField, and later Combobox/DatePicker/Slider in Phase 08) are tar pits to roll by hand — React Aria's `useNumberField` handles locale parsing, decimal separators, scroll wheel, and intl-safe stepper buttons. Split the library by complexity rather than picking one wholesale. |
| 2026-04-17 | Permitted raw `<input>`/`<textarea>` inside form components as the single exception to "no raw HTML" | Form elements uniquely depend on native browser behaviour (value binding, form submission, HTML validation, a11y semantics) that cannot be faithfully composed through `<Box>`. Input/Textarea/NumberInput render their own `<input>` / `<textarea>`; every other component still goes through `<Box>`. Documented in both `07-forms-basic.md` and this log so future contributors don't accidentally "fix" it. |
| 2026-04-17 | Shared control visual recipe in `forms/shared/control.css.ts` (Phase 07) | Input/Textarea/NumberInput all need the same border / bg / focus-ring / invalid / disabled / readOnly / hover visuals. Centralising the wrapper + field + addon/element classes means one place to update visual tokens and guarantees the three text-like controls paint identically. Variants `outline`/`filled`/`ghost` and sizes `sm`/`md`/`lg` live here once. |
| 2026-04-17 | Button's loading state disables + ARIA busies + dims content via `[data-slot="content"]` opacity (Phase 07) | Spinner overlays need the button's width to stay stable so the layout doesn't jump mid-request. Absolute-positioning the spinner and setting `opacity: 0` on the content span (driven by `[data-loading="true"] > [data-slot="content"]` via `globalStyle`) keeps the spinner centered and width intact; also sets `aria-busy="true"` and `disabled` so assistive tech announces the state and clicks are swallowed. |
| 2026-04-17 | `asChild` wraps children in `<Slottable>` so icons + spinner coexist with the consumer element (Phase 07) | The naive `<Slot>{span}{spinner}</Slot>` fails Radix's `React.Children.only` check. Wrapping the user's projected element in `<Slottable>` marks it as the clone target so `leftIcon`/`rightIcon`/`<TinySpinner>` render as siblings of the projected element's children. This also keeps the non-asChild fast path clean — no Slot overhead when rendering a real `<button>`. |
| 2026-04-17 | `switch` thumb size via CSS custom properties (`--lumen-switch-thumb-size`, `--lumen-switch-translate`) rather than per-size thumb recipes | The thumb needs to fit inside each size's track and slide by exactly `track - padding - thumb`. Writing per-size thumb classes would duplicate the style rule × 3. Publishing two numbers per `switchSize` lets one `switchThumb` rule read them with `var()`, and also gives RTL support by negating the translate in `[dir="rtl"] &[data-state="checked"]`. |
| 2026-04-17 | `CheckboxGroup` uses a custom `value: string[]` contract (not Radix's group primitive) | Radix doesn't ship a built-in checkbox-group because checkbox groups aren't a single ARIA role — just a convention. Owning the context means we can express the simple "shared value array" shape cleanly (`value`/`defaultValue`/`onChange`), and children inside a group ignore their local `checked`/`onCheckedChange` in favour of the group's array semantics. |
| 2026-04-17 | React Aria Components for every popover-backed control (Select/Combobox/DatePicker/ColorPicker/Slider) (Phase 08) | Rolling our own popover, listbox, calendar, or slider thumbs invites subtle bugs around virtualised focus, typeahead, touch, RTL, and locale-aware parsing. RAC v1 covers all of these on one dependency; we re-skin with vanilla-extract and layer `size`/`variant` on top. Hand-built components (`MultiSelect`, `FileUpload`, `PinInput`, `TagsInput`, `SearchInput`, `Rating`) are ones Aria doesn't cover or where a simpler bespoke shape is warranted. |
| 2026-04-17 | Shared `forms/shared/popover.css.ts` reused across Select/Combobox/MultiSelect/DatePicker (Phase 08) | Every dropdown-style control needs the same popover shell (positioning, elevation, entrance/exit animation) and the same listbox option styling (hover/selected/focus-visible states). Centralising the recipe keeps the visual grammar identical across controls and cuts duplication — one place to change how selected items paint, and every consumer surface benefits. |
| 2026-04-17 | `LocaleProvider` wraps React Aria's `I18nProvider` (not a parallel context) (Phase 08) | Every locale-aware Aria hook (`useDateFormatter`, `useNumberFormat`, `useLocale`) already reads from Aria's `I18nProvider`. Adding our own locale context would double the tree and create a second source of truth. Re-exporting as `LocaleProvider` + `useLocale` keeps the public API Lumen-shaped while leaving the runtime plumbing to Aria. Default locale is `en-IE` to match Arshad's primary target, overridable by consumers. |
| 2026-04-17 | Hand-built `FileUpload` instead of Aria's `FileTrigger` (Phase 08) | Aria's `FileTrigger` is a button-only trigger; it doesn't include drop zone, validation, preview list, or typed error callback. Building ourselves keeps the API compact (one `<FileUpload>` + optional `<FileUploadTrigger>`/`<FileUploadList>` subcomponents), and drop handling synthesises a change event on the hidden `<input type="file">` so both click and drop go through the same validation pipeline. |
| 2026-04-17 | Bumped the `@lumen/react#build` script to `NODE_OPTIONS='--max-old-space-size=6144' tsup` (Phase 08) | The tsup DTS worker (rollup-plugin-dts) OOMs on GitHub's `ubuntu-latest` runners now that the per-component entry list has grown to ~70 subpaths (11 layout primitives + 7 typography + 29 form controls + hooks + index splits). The default ~4 GB Node heap for worker threads isn't enough to keep the whole type graph resident. Raising the worker heap to 6 GB is cheaper than restructuring the build into multiple passes, and the runner has 7 GB physical RAM. |
| 2026-04-17 | `PinInput` renders N separate `<input maxLength=1>` elements rather than a single masked input (Phase 08) | OTP codes are primarily pasted (one `clipboardData.getData('text')` → distributed across cells) and typed one digit at a time with auto-advance. A single `<input>` with a custom segment renderer loses native paste, caret navigation, and input-method integration (Android's OTP autofill looks for N cells to trigger). N real inputs inherit all of that for free; we just handle arrow-key/backspace focus transfer. `data-filled` + the CSS scheme keeps the "active cell" visual identical per cell regardless of how the content arrived. |
| 2026-04-17 | Radix Primitives (not React Aria) underpin every Phase 09 overlay | Radix's Dialog/Popover/Menu/Tooltip/HoverCard ship exactly the interactions we need — focus trap, portal, scroll lock, collision detection, typeahead — with the smallest per-primitive bundle on npm. React Aria's equivalent pieces are heavier and opinionated about rendering structure. Phase 08 already paid the React Aria tax for complex input controls where it's warranted; overlays are where Radix wins cleanly. |
| 2026-04-17 | `sonner` for Toast instead of rolling one on top of Radix (Phase 09) | `sonner` is ~5 KB gzipped, ships the ergonomic `toast()` call-from-anywhere API (success/error/warning/info/promise, all animated, a11y-announced), and we re-skin it by passing `toastOptions.classNames` derived from our vanilla-extract styles. Writing an equivalent from scratch would cost more code than we'd save. |
| 2026-04-17 | `Modal` is a byte-for-byte alias of `Dialog` (Phase 09) | Consumers and design systems split on naming — Modal vs Dialog. Re-exporting the Dialog module under the Modal names costs nothing at runtime (same module instance, verified by `Modal === Dialog` assertions) and lets both conventions coexist. |
| 2026-04-17 | `Sheet` is an alias of `Drawer` (Phase 09) | Same situation as Modal/Dialog — Sheet and Drawer refer to the same edge-anchored overlay in different design languages. One component, two names, re-exported from a single module. |
| 2026-04-17 | Three CSS recipes power every overlay — `overlay/shared/{overlay,menu,popover}.css.ts` (Phase 09) | Dialog/AlertDialog/Drawer share the backdrop + close button (`overlay.css.ts`). DropdownMenu/ContextMenu/MenuBar share content-shell + items/indicator/separator/shortcut (`menu.css.ts`). Popover/HoverCard share the floating-content shell (`popover.css.ts`). Centralising avoids three parallel variants drifting apart, and the `forms/shared/popover.css.ts` recipe is deliberately separate because Listbox has different concerns from a generic Popover. |
| 2026-04-17 | AlertDialog blocks Escape via `onEscapeKeyDown={e => e.preventDefault()}` in our wrapper (Phase 09) | Radix's `AlertDialogContent` already omits `onInteractOutside` / `onPointerDownOutside` from its props — backdrop clicks never dismiss. But Escape is allowed by default. Destructive-action confirmation should be an explicit Action-or-Cancel choice, so we suppress Escape in the wrapper to deliver the contract the spec requires. |
| 2026-04-17 | `ScrollLock` is a ref-counted utility the overlays don't actually use (Phase 09) | Radix's Dialog/AlertDialog/Drawer already lock body scroll internally — wiring our own over the top would race with theirs. We still export `lockBodyScroll` for consumers building custom (non-Radix) overlays, with ref counting so multiple stacked locks cooperate. Tested in isolation. |
| 2026-04-17 | jsdom polyfills moved into `src/test/setup.ts` (Phase 09) | Sonner touches `matchMedia` at mount; Radix Tooltip/Popover touch `ResizeObserver`; Radix Menu touches `scrollIntoView`/`hasPointerCapture`. Polyfilling once at setup time beats sprinkling stubs across every overlay test. Tests that want finer control (e.g. `useMediaQuery`) still override `window.matchMedia` locally. |
| 2026-04-17 | Radix underpins Tabs + NavigationMenu, everything else is hand-built (Phase 10) | Tabs and NavigationMenu have a Radix package that already nails keyboard navigation, focus trapping inside panels, hover intent, and RTL flipping — re-skinning is cheaper than re-deriving. Breadcrumb/Pagination/Menu/Stepper/Anchor/BackToTop/Sidebar are plain HTML + our own logic; none of them benefit from an external primitive (Sidebar orchestrates our Phase 09 Drawer on mobile instead of pulling in another dep). |
| 2026-04-17 | `paginationRange` algorithm mirrors MUI's `usePagination` (Phase 10) | MUI's algorithm is battle-tested, covers the "extend the sibling window near the boundaries" case (so visible-count stays roughly constant as `currentPage` scans the range), and doesn't produce asymmetric outputs. Shipped as a pure export (`@lumen/react/pagination` re-exports it) so consumers can paint their own paginator without re-deriving the math. |
| 2026-04-17 | `Sidebar` switches to `<Drawer>` below a media-query breakpoint instead of keeping a collapsible rail on mobile (Phase 10) | Icon rails waste touch-sized screens, and the drawer pattern is what native apps do. `SidebarProvider` owns both the desktop `collapsed` state and the mobile `mobileOpen` state behind one `SidebarTrigger` that picks the right behaviour via `useIsMobile`. The media query is the only thing the consumer tunes. |
| 2026-04-17 | `TabsIndicator` measures via getBoundingClientRect + MutationObserver, writes `--lumen-tabs-indicator-*` custom props (Phase 10) | The animated underline needs to follow the active trigger through layout shifts (font loads, container resizes, and `data-state` flips from Radix). ResizeObserver + a MutationObserver on `data-state` covers all three. Writing four custom properties instead of inline `left/width` inlines the data once per render and lets the CSS transition own the animation — reduced-motion just zeroes the duration. |
| 2026-04-17 | `BreadcrumbSeparator` renders as `<li role="presentation" aria-hidden>` so the `<ol>` semantics stay intact (Phase 10) | The `<nav><ol><li>...</li></ol></nav>` tree is the canonical breadcrumb markup. Separators are pure decoration and must not add to the item count; `role="presentation"` + `aria-hidden` removes them from the AT tree so screen readers announce N items, not N + (N-1). Tested for the 3-items-in-the-list case. |
| 2026-04-17 | Hand-rolled `Tree` instead of React Aria's `Tree` (Phase 11) | React Aria's `Tree` collection is built around its `Collection`/`Item` API, which would have forced every consumer through `<TreeItem id>` children + their rendering lifecycle. A render-prop API driven by a plain `TreeNode[]` recursion is a much simpler mental model, preserves our "compose primitives" grammar, and the keyboard-navigation contract (ArrowUp/Down/Left/Right/Home/End/Enter/Space/`*`) is well-defined enough to implement in ~200 LOC on top of a DFS-flattened view of visible items. Single + multiple selection and `aria-level`/`aria-expanded`/`aria-selected` are wired for screen readers; expand-all on `*` operates on the focused node's sibling group per ARIA APG. |
| 2026-04-17 | TanStack Table v8 powers `DataTable`; hand-written rendering on top of the static `Table` (Phase 11) | TanStack gives us sorting / filtering / pagination / selection state for free and is the de-facto standard headless table. Wiring the header → sort button, the body → rows + cells, and the select column through our own `TableHeader`/`TableCell` keeps the visual layer on the shared Lumen recipe (so `variant="striped"` etc. still apply). Virtualisation + column resize are deferred; the exit criterion's "10 000-row demo" goal hinges on both — Phase 14 or a follow-up minor revisits them rather than letting the scope balloon here. |
| 2026-04-17 | `CodeBlock` ships a plain renderer by default and a `createShikiRenderer` / `useShikiRender` helper that dynamically `import('shiki')` (Phase 11) | Bundling Shiki (~1 MB worth of grammars + regex engine + themes) into every consumer that renders a code block is unreasonable. The plain renderer covers the "just display source" case without any highlighter dependency; consumers that want full syntax colouring import via `@lumen/react/code-block` and opt into Shiki at their own bundle boundary. `CodeBlock` accepts pre-rendered HTML via an `html` prop so the highlight can be computed at build time, request time, or on a Shiki Web Worker — Lumen doesn't dictate. |
| 2026-04-17 | `Modal` / `Sheet` / `Splitter` / `Disclosure` are byte-for-byte aliases (Phases 09, 11) | Design systems differ on naming conventions; re-exporting the same module under multiple names is zero-cost at runtime (same module instance) and lets consumers use whichever vocabulary their codebase already speaks. Documented asserts (`Splitter === Resizable`, `Disclosure === Collapsible`) keep the pairs honest. |
| 2026-04-17 | Accordion uses Radix; Collapsible uses Radix; ScrollArea uses Radix; Resizable uses `react-resizable-panels`; DataTable uses TanStack (Phase 11) | Each of these has a single obvious best-in-class primitive with a stable API. Rolling our own would be weeks of subtle keyboard / a11y / resize / height-animation bugs for no differentiation. The CSS is ours end-to-end (vanilla-extract recipes over the shared token contract), so theming and visual parity with the rest of the library is unaffected. Bundle cost is externalised to the peer list already tracked in the tsup config. |
| 2026-04-17 | ScrollArea viewport child override uses `globalStyle` + `display: block !important` (Phase 11) | Radix's `ScrollArea.Viewport` wraps its children in a `<div>` with an inline `display: table` style so it can be measured; that breaks percentage-sized descendants which expect flow layout. A `globalStyle` override scoped to the viewport's direct `div[style]` child restores block formatting. The inline descendant selector can't live inside vanilla-extract's nested `selectors` because it doesn't start with `&`; `globalStyle` is the sanctioned escape hatch. |
| 2026-04-17 | `Tree.expandedIds` / `selectedIds` go through `useControllableState` (Phase 11) | Both props expose controlled+uncontrolled shapes (`expandedIds`/`defaultExpandedIds`, `selectedIds`/`defaultSelectedIds`) identical to every other stateful Lumen component. `useControllableState` is the single source of truth for that pattern; wiring a second one-off state machine per component would guarantee divergence. |
| 2026-04-17 | `DataTable` emits `onSelectionChange(rows)` via an effect synced from the internal row-selection map (Phase 11) | TanStack's selection state is a `Record<string, boolean>` keyed by row id — useful for its internals, awkward as the library's outward API. Deriving `TData[]` from the selected rows inside a `useMemo` and calling `onSelectionChange` when the set changes gives consumers the shape they want (rows, not a Record) without re-fetching the row model on every render. |
| 2026-04-17 | Three separate components for Badge / Tag / Chip instead of one `variant`-driven component (Phase 12) | The three have genuinely different semantics: Badge is non-interactive label/count next to something, Tag is a categorical label that is optionally interactive / optionally removable, and Chip is always an interactive toggle (`aria-pressed`). Folding them into a single "Tag" would have forced branches on every prop (`interactive`, `selectable`, `removable`) where the correct DOM element, ARIA attributes, and keyboard behaviour all differ. Three components keep each consumer's intent unambiguous, matches the spec's clarity rule, and the per-component CSS recipe still reuses `Badge.css.ts` for the visual layer. |
| 2026-04-17 | `Avatar` colour-from-name hash uses a fixed 8-colour palette + deterministic `char * 31 + charCodeAt` rolling hash (Phase 12) | The palette size trades off visual variety vs. predictable contrast (too many colours → indistinguishable pastel soup). Eight maps cleanly to our `feedback.*.soft` + `accent.soft` tokens so every initials avatar inherits a theme-aware surface. The hash function is stable, cheap, and order-sensitive (so "Bob" ≠ "obB"); consumers can still override per-avatar via `colorScheme`. Exported `colorFromName` and `initialsFromName` helpers so consumers can pre-compute colours for server-side rendering without mounting an Avatar. |
| 2026-04-17 | Radix for Avatar + Toggle + ToggleGroup, hand-built everything else in Phase 12 (Phase 12) | `@radix-ui/react-avatar` handles the tricky `<img>` load state + fallback timing that we'd otherwise bolt on with a load listener; `@radix-ui/react-toggle`/`-toggle-group` ship the `aria-pressed`/`role="radiogroup"` + arrow-key navigation for free. Badge/Tag/Chip/Alert/Banner/Notification/Callout/EmptyState are simple compositions of our own CSS surface over `<span>`/`<div>`/`<button>` elements and don't warrant a dependency — rolling them gets us cleaner APIs and a smaller runtime tree. |
| 2026-04-17 | `Banner.dismissKey` persists a `1` value under the `lumen:banner:<key>` namespace in `localStorage`, swallowing errors (Phase 12) | The namespace prefix avoids colliding with any consumer keys while staying greppable. Wrapping reads and writes in try/catch covers: Safari's "localStorage is undefined in private mode" throw, quota errors, and the SSR case where `window.localStorage` simply doesn't exist. A dedicated `clearBannerDismissal(key)` helper ships for test suites (and consumers that need to "reset the onboarding flow"), instead of asking them to reconstruct the key namespace themselves. |
| 2026-04-17 | Shared `feedback/shared/surface.css.ts` recipe powers Alert / Banner / Notification / Callout's `variant × status` cells (Phase 12) | All four components draw from the same background + border + foreground tokens per severity — only their padding, shape, and layout differ. Centralising the recipe guarantees that "soft success" looks identical regardless of which surface renders it, and halves the compiled CSS for the variant matrix (one `softSuccess` class referenced four times vs. four near-identical classes). The three layout-distinct shells (`Alert`, `Banner`, `Notification`, `Callout`) still own their own root CSS because their spacing/typography rules genuinely diverge. |
| 2026-04-17 | `ToggleGroupAttached` uses `globalStyle` to reach into `toggleRoot` from its parent class (Phase 12) | vanilla-extract's nested `selectors` can only target the style being defined (via `&`). The attached pill-bar visual needs to collapse borders of every child `toggleRoot` to shared inline separators, which requires a cross-class descendant selector. `globalStyle(parent descendant, …)` is the sanctioned escape hatch (same pattern Phase 11 used for Radix's `ScrollArea.Viewport`); consumers don't see it because the class they apply is still `toggleGroupAttached`. |
| 2026-04-17 | `FormControl` clones its single child and injects `id` / `name` / `aria-describedby` / `aria-invalid` / `disabled` / `required` / `invalid`, with any existing prop on the child winning (Phase 13) | The spec's "clone-not-wrap" design preserves the DOM structure — the input stays a direct sibling of the label/description/message inside the field stack, so `:has()` selectors, sibling combinators, and floating-label patterns still work. Letting the child override everything means consumers can escape the inherited `disabled={true}` or target a different field id when they need to without layering a second wrapper. Existing `aria-describedby` on the child is preserved by concatenation (space-separated) with the registered description/message ids, matching the ARIA spec for multi-id describedby. |
| 2026-04-17 | `FormField` uses a plain `<div>` + vanilla-extract class, not `Stack` (Phase 13) | `Stack` extends `LayoutProps` which defines its own `color` prop as `Responsive<ColorToken>`; `FormField`'s caller-facing `HTMLAttributes<HTMLDivElement>` contract carries the native HTML `color` attr as `string`. Spreading `...rest` onto `Stack` would collide on `color`, forcing a `Omit` of every layout-prop key from the public `FormFieldProps`. A single-purpose `formField` class (flex column + `space.1.5` gap) is clearer, trims the component's bundle, and matches how other focused-semantics wrappers (Label, HelperText, ErrorText) are implemented in the package. |
| 2026-04-17 | `react-hook-form` is an **optional** peer dependency (`peerDependenciesMeta.optional: true`) and `@lumen/react/rhf` imports RHF statically (Phase 13) | The `/rhf` subpath is the *only* code path that reaches for `react-hook-form`; consumers who never import it never see the dependency at build time or runtime. A static `import` (rather than a lazy `require` guard) keeps the module tree-shakeable and lets TypeScript flow the full RHF generics into `RHFField<TValues, TName>`. Making the peer optional prevents npm / pnpm from warning the 99 % of consumers who don't use RHF; the 1 % who do are on the hook to install it, and `RHFField`'s first call will throw a clear module-not-found if they don't. |
| 2026-04-17 | `FormMessage` registers its id on `aria-describedby` only when it has content and emits `role="alert"` only when `FormField.invalid` is true (Phase 13) | A permanently-mounted message slot with no content shouldn't pollute `aria-describedby` — screen readers would still follow the empty reference and announce nothing, wasting cycles. Tying `role="alert"` to the invalid flag (not the mere presence of text) means passive "helper-as-warning" copy doesn't announce itself; only actual validation failures do. Both behaviours make the common pattern `<FormMessage>{errors.field?.message}</FormMessage>` correct by construction. |
| 2026-04-17 | Phase 14 RTL audit is scoped to `*.css.ts` only; consumer layout props (`paddingLeft`, `marginRight` on `<Box>`) stay physical (Phase 14) | The spec's canonical grep (`grep -rE '(margin\|padding\|border)-(left\|right)'`) flags every physical-intent escape hatch in the public `LayoutProps` API — `pl`, `pr`, `ml`, `mr` props and their `types.ts` / `resolveLayoutProps.ts` wiring. Those are intentional and must stay physical (callers who want logical behaviour use `paddingInline`, `marginInline`, or write inline styles). Restricting the audit to authored CSS (`.css.ts`) catches the only files where a style author can accidentally ship a non-RTL-safe rule, while the allowlist documents the two files where physical properties are the correct answer (`layoutStyle.css.ts` registry, `Drawer.css.ts` side-anchored borders). |
| 2026-04-17 | Phase 14 contrast audit walks compiled CSS, not source JSON, and composites alpha-blended foregrounds over their background before measuring (Phase 14) | The script must check what consumers actually ship, and Style Dictionary's compiled CSS is the single source of truth post-phase-02. Reading `.json` sources would miss the preprocessor pipeline (`lumen/expand-typography`, reduced-motion appending) and reinvent `var()` resolution. Alpha compositing matters because the terminal theme's `accent-soft` / feedback-soft surfaces use `rgba()` — measuring the raw alpha-channel colour would overstate the contrast versus what actually paints. Six themes × 19 pairs = 114 assertions per run. |
| 2026-04-17 | Cross-browser Vitest is opt-in via `LUMEN_BROWSER_MODE=1`, not always-on (Phase 14) | Playwright's chromium + firefox + webkit install is ~350 MB and every test mounts three browser contexts. Running it on every `pnpm test` locally would kill the edit/test loop. The env var gate keeps the default fast (jsdom → Chromium-only browser mode is also available via `test:browser`), while CI's `cross-browser` job opts in unconditionally. |
| 2026-04-17 | `size-limit` budgets live in a single root `.size-limit.json`, not per-package (Phase 14) | One file, one source of truth. `size-limit` resolves `path` relative to repo root, so point at `packages/react/dist/*.js` and we're done. Per-package configs would duplicate the ignore list (`react`, `react-dom`) 44 times and split the "one command tells me every regression" output that CI reports in a single step. |
| 2026-04-17 | Storybook 10 is the sole docs tool; no separate Docusaurus/Astro site (Phase 15) | Storybook's MDX-in-Docs-mode covers every docs requirement (prose pages, interactive component playgrounds, auto-generated prop tables via `react-docgen-typescript`, search, responsive preview, themed canvas). A parallel Docusaurus/Next site would duplicate effort (keeping examples in sync with components) and split the audit surface (a11y addon, Chromatic). One source of truth wins. Foundation pages live under `docs/foundations/`, recipes under `docs/recipes/`, and the build picks them up via the updated `stories` glob in `.storybook/main.ts`. |
| 2026-04-17 | Cloudflare Pages over Vercel for docs hosting (Phase 15) | Matches Arshad's existing `arshadshah.com` infra; zero additional vendors. The site is a static `storybook-static/` directory — no SSR needed, no edge-functions needed, no Next-specific advantage. Deploys run through Cloudflare Pages' Git integration (Pages builds on its own runners, reads `pnpm build && pnpm docs:changelog && pnpm build-storybook`, publishes `storybook-static/`), so no GitHub Actions workflow is needed. `wrangler.toml` stays at the repo root for `wrangler pages deploy` + `wrangler pages dev` runs. |
| 2026-04-17 | `scripts/build-changelog-page.mjs` writes `docs/reference/changelog.mdx` idempotently (Phase 15) | CI runs this step after `changeset version` bumps. Comparing against the existing file and skipping the write when unchanged keeps git diffs tidy and means the script can run on every PR (not just releases) without producing churn. Skips private packages + packages with empty CHANGELOGs so the output stays meaningful. |
| 2026-04-17 | Chromatic action pinned at major `chromaui/action@v16` (Phase 16) | `v16` is the current major line and runs under Node 24 — matches the repo's Node 22+ requirement (workflow explicitly uses Node 22). `@v11` was the old pin and reached end of life. We pin the major (not `@latest`) so the workflow is deterministic; bumping is a single-line change when the next major ships. The `chromatic` CLI is pinned in the workspace catalog at `16.3.0` for consistency between local `pnpm chromatic` runs and the action's pinned CLI. |
| 2026-04-17 | Release workflow runs `typecheck`, `test`, `publint`, `attw` before handing off to Changesets (Phase 16) | The `verify` CI job already enforces these on PRs, but a release tagged from `main` could theoretically drift (e.g. a lockfile-only commit that skipped the `verify` path). Repeating the checks inside `.github/workflows/release.yml` is cheap (Turbo-cached) and cuts off the "how did that broken release ship?" class of incident. |
| 2026-04-17 | Build-spec markdown relocated from repo root to `docs/specs/` (Phase 16) | Phase 01–14 lived at the repo root as top-level files (`01-foundation.md`, `ARCHITECTURE.md`, `PROGRESS.md`). That was fine when the audience was "Claude Code, phase by phase". For the public v1.0.0 cut, consumers scanning the root see `README.md` + `LICENSE` + `CONTRIBUTING.md` as they expect, and everything else is organised under `docs/`. `docs/specs/` retains the exact files the spec-driven workflow relies on; internal links were preserved (`docs/specs/phases/...`). |

---

## Open questions / risks

Things that aren't blocking yet but need answering before they become blocking.

- [ ] _example: Do we need a Vue port? If so, extract `@lumen/core` further._
- [ ] _example: Chromatic budget — is $149/mo justified, or use Playwright visual comparison locally?_

---

## Change log

Append every commit here. **This is your audit trail.** One line per commit, newest at the top.

Format:
```
<date> · <short-hash> · <scope> · <one-sentence summary> · [changeset: <level>]
```

Example:
```
2026-04-17 · a1b2c3d · @lumen/react · feat(phase-05): add Stack layout primitive with space/direction variants · [changeset: minor]
2026-04-17 · e4f5g6h · @lumen/tokens · chore(phase-02): bump spacing scale to 4-based · [changeset: patch]
```

### Entries

<!-- newest at top; append after every commit -->

<!-- commit hashes appended after `git commit` lands each chunk; see `git log --oneline` for the canonical record -->
- 2026-04-17 · repo,@lumen/react · feat(phase-15+16): documentation site and release readiness — Storybook-as-docs-site with MDX foundations (11 pages) + priority recipes (8) + guides/reference/auto-generated changelog; `.storybook/main.ts` picks up `docs/foundations/**` + `docs/recipes/**` with tuned `react-docgen-typescript`; `.storybook/manager.ts` branded dark theme (terminal palette + JetBrains Mono); `scripts/build-changelog-page.mjs` aggregates `packages/*/CHANGELOG.md`; `wrangler.toml` for `wrangler pages deploy` + local dev (Cloudflare Pages Git integration handles prod); `.github/workflows/release.yml` runs typecheck/test/publint/attw pre-publish; Chromatic bumped `chromaui/action@v11` → `@v16` + `chromatic` CLI pinned 16.3.0 (+ `pnpm chromatic` root script); `LICENSE` (MIT) / `CONTRIBUTING.md` / `CODE_OF_CONDUCT.md` / `SECURITY.md`; consumer-facing `README.md`; build-spec markdown moved to `docs/specs/` [changeset: patch]
- 2026-04-17 · repo,@lumen/react · feat(phase-14): testing & quality hardening — `.size-limit.json` with 44 per-component budgets + `pnpm size` root script; `scripts/audit-rtl.mjs` (CSS-TS-only, Drawer + layoutStyle registry allowlisted with rationale) + `scripts/audit-contrast.mjs` (colorjs.io, 6 themes × 19 pairs WCAG 2.1 AA, alpha-compositing); `packages/react/vitest.config.ts` coverage thresholds (85/85/80/85 lines/functions/branches/statements) + opt-in cross-browser (LUMEN_BROWSER_MODE=1 → Chromium+Firefox+WebKit); `.storybook/preview.ts` axe locked to WCAG 2.1 A+AA tags, still `test='error'`; nine `.css.ts` files rewritten from physical to logical CSS props (Callout borders, Tag remove button, Divider vertical variants, NavigationMenu arrow, Tabs vertical separator, CodeBlock line numbers, overlay/shared menu items, feedback/shared surface close button); CI workflow split into `verify` (lint/typecheck/audit:rtl/build/test/coverage/audit:contrast/size/publint/attw) + opt-in `cross-browser` job with playwright; `.github/renovate.json` (radix/react-aria/vanilla-extract/storybook/vitest/tanstack groups, Monday schedule); `.github/workflows/chromatic.yml` + `lighthouserc.json` ≥ 0.95 a11y / ≥ 0.85 perf [changeset: patch]
- 2026-04-17 · @lumen/react · feat(phase-13): form composition — `Form` (thin `<form>` wrapper, `noValidate` default `true`), `FormField` (useId + context + vertical stack), `FormLabel` (auto `htmlFor` + inherited `required` indicator), `FormControl` (clone-element injecting `id`/`name`/`aria-describedby`/`aria-invalid`/`disabled`/`required`/`invalid`, child props win + describedBy concatenated), `FormDescription` (registers `-description` id), `FormMessage` (registers `-message` id, renders nothing on empty children, `role="alert"` when invalid); `@lumen/react/rhf` subpath with `RHFField` adapter over `useController`; `react-hook-form` declared as optional peer; three Storybook MDX recipes (plain / RHF / RHF + Zod); per-component tsup entries for `form` + `rhf` + Node10 sidecars; 17 new unit + integration tests (409/409 total pass) [changeset: minor]
- 2026-04-17 · @lumen/react · feat(phase-12): feedback components — Badge/Tag/Chip (three explicit semantics sharing one visual recipe), Avatar + AvatarGroup (Radix `react-avatar`, deterministic `colorFromName` hash, status dots, `+N` overflow tile), Alert + AlertTitle + AlertDescription (role default = `alert` for danger/warning else `status`), Banner + BannerContent + BannerActions (`dismissKey` persists in `localStorage` via `lumen:banner:<key>` namespace + `clearBannerDismissal` helper), Notification (unread/actions/timestamp inline card), Callout + CalloutTitle + CalloutContent, EmptyState + EmptyStateIcon + EmptyStateTitle + EmptyStateDescription + EmptyStateActions, Toggle + ToggleGroup + ToggleGroupItem (Radix `react-toggle` + `react-toggle-group`, shared `ToggleContext` for size/variant); shared `feedback/shared/{surface.css.ts,icons.tsx,variants.ts}` recipes; 3 new Radix deps (`react-avatar`, `react-toggle`, `react-toggle-group`); per-component tsup entries + Node10 shims; 35 new unit tests (392/392 total pass) [changeset: minor]
- 2026-04-17 · @lumen/react · feat(phase-11): data-display components — Card family, Table (static) + DataTable (TanStack-backed sort/select/paginate/filter/loading/empty), Tree (hand-rolled, full keyboard nav + single/multi select), Timeline, Stat, Progress + ProgressCircle (striped/animated/indeterminate), Skeleton, Spinner (border/dots/ring), Accordion + Collapsible + Disclosure (Radix), ScrollArea (Radix), Resizable + Splitter (react-resizable-panels), CodeBlock (plain renderer + optional lazy Shiki via `/code-block` entry); 5 new deps (`@radix-ui/react-accordion`, `@radix-ui/react-collapsible`, `@radix-ui/react-scroll-area`, `@tanstack/react-table`, `react-resizable-panels`, `shiki`); per-component tsup entries + Node10 shims; 35 new unit tests (357/357 total pass) [changeset: minor]
- 2026-04-17 · @lumen/react · feat(phase-10): navigation components — Tabs (Radix + animated TabsIndicator), Breadcrumb (+ `maxItems` collapse), Pagination (+ pure `paginationRange` util), Menu (sidebar-style polymorphic), NavigationMenu (Radix-backed mega-menu with viewport/indicator), Sidebar (`SidebarProvider` + mobile Drawer fallback), Stepper, Anchor (copy-link heading), BackToTop; two new Radix deps (`react-tabs`, `react-navigation-menu`); per-component tsup entries + Node10 shims; 36 new unit tests (322/322 total pass) [changeset: minor]
- 2026-04-17 · @lumen/react · feat(phase-09): overlays — Dialog/AlertDialog/Modal, Drawer/Sheet, Popover, HoverCard, Tooltip+TooltipProvider, Toast (sonner-backed Toaster + re-exported `toast`), DropdownMenu/ContextMenu/MenuBar; shared `overlay/shared/{overlay,menu,popover}.css.ts` recipes; `ScrollLock` + `OverlayPortal` + `useOverlayMount` helpers; per-overlay tsup entries + Node10 shims; jsdom polyfills (matchMedia/ResizeObserver) centralised in test setup; 28 new overlay unit tests (286/286 total pass) [changeset: minor]
- 2026-04-17 · @lumen/react · feat(phase-08): advanced form controls — Select/Combobox/MultiSelect, Slider/RangeSlider, DatePicker/DateRangePicker/TimePicker, ColorPicker, FileUpload, SearchInput, PinInput, TagsInput, Rating; LocaleProvider around React Aria I18nProvider; shared `forms/shared/popover.css.ts` listbox/popover recipe; `@internationalized/date` direct dep; per-component tsup entries + Node10 shims; 34 new unit tests (258/258 total pass) [changeset: minor]
- 2026-04-17 · @lumen/react · feat(phase-07): basic form controls — Button/IconButton/ButtonGroup, Input/Textarea/NumberInput, Checkbox/CheckboxGroup/Radio/RadioGroup/Switch, Label/HelperText/ErrorText/Fieldset; shared FormControlBase contract + shared control.css.ts visual recipe; Radix for boolean primitives, react-aria-components for NumberField; per-component tsup entries + Node10 shims; 40 new unit tests [changeset: minor]
- 2026-04-16 · @lumen/react,@lumen/tokens · feat(phase-06): typography components — Text/Heading/Code/Kbd/Link/Blockquote/List+OrderedList+DescriptionList composed on Box; responsive size/weight/align via CSS custom property cascades; composite `font.heading.*`/`font.body.*` tokens now emitted in CSS (preprocessor filePath propagation); per-component tsup entries + Node10 shims; 41 new unit tests [changeset: minor + patch]
- 2026-04-16 · @lumen/react · feat(phase-05): 11 layout primitives on vanilla-extract — Box/Stack/Inline/Flex/Grid/Center/Spacer/Divider/AspectRatio/Container/Section, shared responsive `LayoutProps` → CSS-variable pipeline, per-primitive tsup entries + Node10 shims, Storybook gallery + 33 unit tests [changeset: minor]
- 2026-04-16 · @lumen/react · feat(phase-04): core utilities — Slot/Portal/VisuallyHidden primitives, cn/composeRefs/composeEventHandlers/callAll/createContext/cva utils, 21 hooks, per-hook tsup entries, Storybook + playground demos [changeset: minor]
- 2026-04-16 · @lumen/react,@lumen/themes,@lumen/tokens · feat(phase-03): runtime-free theming system — ThemeProvider, DirectionProvider, hooks, getThemeInitScript; prebuilt terminal + high-contrast themes; breakpoint tokens, focus shadow, reduced-motion CSS [changeset: minor × 3]
- 2026-04-16 · @lumen/tokens · feat(phase-02): DTCG token pipeline (primitives + semantic light/dark, Style Dictionary v4 CSS+TS output, Ajv schema validator) [changeset: minor]
- 2026-04-16 · repo · chore(phase-01): complete Phase 01 foundation scaffold (monorepo, tooling, Storybook, Vitest, tsup, CI, playground)

---

## Release history

Populated automatically by Changesets, but mirror major releases here for quick reference.

| Version | Date | Highlights |
|---------|------|------------|
| _none yet_ | | |

---

## Dependencies watch

Monitor these for security advisories and major version bumps.

- React (peer)
- Radix Primitives (direct)
- React Aria Components (direct)
- Vanilla Extract (build-time)
- Storybook (dev)
- Vitest (dev)
- Biome (dev)
