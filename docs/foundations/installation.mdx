import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Getting started/Installation" />

# Installation

## Requirements

- Node 22 or newer (Cynosure is authored against ESM and uses native TypeScript
  loading where possible).
- React 19 and React DOM 19 as peer dependencies.

## Install

```bash
pnpm add @arshad-shah/cynosure-react @arshad-shah/cynosure-tokens
```

Or with npm / yarn:

```bash
npm install @arshad-shah/cynosure-react @arshad-shah/cynosure-tokens
yarn add @arshad-shah/cynosure-react @arshad-shah/cynosure-tokens
```

## Import the CSS

Cynosure ships CSS separately from JS so bundlers can tree-shake unused
components. Import the tokens + component styles once at the root of your
app:

```ts
import '@arshad-shah/cynosure-tokens/css';          // design tokens, light theme
import '@arshad-shah/cynosure-tokens/css/dark';     // dark overrides (optional)
import '@arshad-shah/cynosure-react/styles.css';    // component styles barrel
```

### Per-component CSS

If you want to import only the components you use:

```ts
import { Button } from '@arshad-shah/cynosure-react/button';
import '@arshad-shah/cynosure-react/button/button.css';
```

Your bundler wires up the CSS automatically when you use the per-component
ESM entries.

## Optional themes

```ts
import '@arshad-shah/cynosure-themes/terminal';
import '@arshad-shah/cynosure-themes/high-contrast';
```

## TypeScript

Cynosure is authored in TypeScript and publishes types with full JSDoc. If you
use `skipLibCheck: false`, make sure your `lib` includes `DOM` and `DOM.Iterable`.

## Peer dependencies

| Peer               | Range            | Required |
| ------------------ | ---------------- | -------- |
| `react`            | `^19`            | yes      |
| `react-dom`        | `^19`            | yes      |
| `react-hook-form`  | `^7`             | only if you import `@arshad-shah/cynosure-react/rhf` |

See the [quickstart](?path=/docs/getting-started-quickstart--docs) for a
minimal end-to-end example.
