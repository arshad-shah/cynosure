import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Foundations/Theming/Dark mode" />

# Dark mode

Dark mode in Lumen is a semantic-token override, not a component prop. You
opt in once at the root and every component follows.

## 1. Import the dark stylesheet

```ts
import '@lumen/tokens/css';          // base + light
import '@lumen/tokens/css/dark';     // dark overrides
```

## 2. Allow dark in `<ThemeProvider>`

```tsx
<ThemeProvider themes={['light', 'dark']} defaultTheme="system">
  {children}
</ThemeProvider>
```

`defaultTheme="system"` matches the OS / browser preference via
`prefers-color-scheme: dark`. Users can override with `setTheme('dark')`
and Lumen persists the choice.

## 3. Flash-free on first paint

Render the init script in the document `<head>` so the theme is applied
before React hydrates — this avoids a light-then-dark flash on reload.

```tsx
import { getThemeInitScript } from '@lumen/react';

<script dangerouslySetInnerHTML={{ __html: getThemeInitScript() }} />
```

## How it works

`@lumen/tokens/css/dark` scopes its overrides to `[data-theme='dark']`.
`ThemeProvider` writes `data-theme="dark"` on the `<html>` element when the
resolved theme is dark. Every Lumen component reads its colours from
semantic tokens like `--lumen-color-accent-solid` which are re-pointed by
the dark stylesheet.

The practical implication: consumer CSS that uses Lumen tokens gets dark
mode for free. No `.dark .my-component { … }` rules to write.

## Colour scheme + form controls

`ThemeProvider` also sets the CSS `color-scheme` property to match the
resolved theme. That hints the browser to render form controls, scrollbars,
and the native `:focus` ring in the correct palette.

## Caveats

- **Images and media.** Tokens cover colour; media you ship (screenshots,
  illustrations) may need light + dark variants. Use the CSS
  `prefers-color-scheme` media query or a React hook (`useTheme()`) to
  pick.
- **Third-party components.** Components outside Lumen read their own
  colour system. Wrapping them in Lumen tokens requires a small CSS bridge.
